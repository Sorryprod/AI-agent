# AI Browser Agent

Автономный AI-агент для управления браузером с поддержкой сложных многошаговых задач.

## Возможности

- 🌐 **Автоматизация браузера** через Playwright
- 🔐 **Persistent Sessions** - логинитесь один раз, сессия сохраняется
- 🛡️ **Security Layer** - подтверждение перед опасными действиями
- 🧠 **ReAct паттерн** - агент думает перед каждым действием
- 🔄 **Адаптивная обработка ошибок** - пробует альтернативные подходы
- 📊 **Умное извлечение контента** - экономия токенов

## Установка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd browser-agent

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Установите браузеры Playwright
playwright install chromium

# Настройте API ключ
cp .env.example .env
# Отредактируйте .env и добавьте ANTHROPIC_API_KEY