🤖 AI Browser Copilot
Ваш персональный ИИ-ассистент, встроенный прямо в браузер.
Это не просто скрипт автоматизации — это Копилот, который живет в боковой панели Chrome. Вы можете пользоваться браузером как обычно, а когда станет лень — просто напишите задачу в чат.

AI Status
Browser
Status

✨ Особенности
🧠 Умный агент: Работает на базе Google Gemini 1.5 Flash (быстро и бесплатно).
🎨 Нативный интерфейс: Красивая боковая панель (Side Panel) в стиле Glassmorphism.
⚡ Гибридный режим: Браузер открыт для вас. Работайте руками или делегируйте задачи ИИ.
💾 Память: История чата сохраняется. При перезапуске агент помнит контекст.
🚀 Производительность: Оптимизированный анализ страниц (экономит токены и трафик).
🛡️ Безопасность: Спрашивает разрешение перед удалением данных или оплатой.
🛠️ Установка (Локально - Рекомендуется)
Используйте этот способ, чтобы видеть окно браузера и работать вместе с агентом.

1. Подготовка
Bash

# 1. Клонируйте репозиторий
git clone <ваш_репозиторий>
cd browser-agent

# 2. Создайте виртуальное окружение
python -m venv venv

# 3. Активируйте окружение
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Установите браузер Chromium
playwright install chromium
2. Настройка ключей
Получите бесплатный ключ Google AI Studio.
Создайте файл .env в корне проекта и вставьте ключ:
env

GOOGLE_API_KEY=AIzaSy...ваш_ключ...
3. Запуск сервера
Bash

python server.py
После запуска автоматически откроется окно браузера Chromium.

4. Установка расширения в Chrome
Чтобы появилась боковая панель управления:

Откройте открывшийся браузер (или свой основной Chrome).
Перейдите по адресу: chrome://extensions
В правом верхнем углу включите тумблер "Режим разработчика" (Developer mode).
Нажмите кнопку "Загрузить распакованное расширение" (Load unpacked).
Выберите папку extension из этого проекта.
В панели расширений (значок "пазл") закрепите AI Browser Agent.
Кликните по иконке — откроется боковая панель.
🐳 Запуск через Docker (Фоновый режим)
Используйте этот способ, если вам нужен агент как сервис (без графического интерфейса). Вы сможете управлять им через расширение в своем обычном браузере.

Bash

# Запуск контейнера
docker-compose up --build
После запуска подключитесь к ws://localhost:8000/ws через расширение, установленное в вашем локальном браузере.

🖥️ Как использовать
Откройте боковую панель (клик по иконке расширения).
Введите задачу, например:
"Открой hh.ru и найди вакансии Python Junior"
"Зайди в Яндекс Лавку и добавь молоко в корзину"
"Найди в Google погоду в Москве на завтра"
Следите за процессом. Агент будет писать свои действия в чат.
Вмешательство. В любой момент вы можете кликнуть по странице сами или нажать кнопку STOP в панели.
📂 Структура проекта
text

browser-agent/
├── agent/                  # Логика ИИ
│   ├── ai_agent.py         # Мозг (Gemini + ReAct)
│   ├── browser_controller.py # Управление Playwright
│   ├── page_analyzer.py    # Анализ DOM (Lite версия)
│   └── tools.py            # Инструменты
├── extension/              # Chrome Extension (Frontend)
│   ├── manifest.json
│   ├── panel.html          # UI (HTML/CSS)
│   ├── panel.js            # Логика UI
│   └── background.js       # Связь с браузером
├── server.py               # Бэкенд (FastAPI + WebSocket)
├── config.py               # Настройки
└── Dockerfile              # Конфигурация контейнера
❓ Решение проблем
Ошибка Server disconnected или Timeout:
Возможно, у вас медленное соединение с Google API через VPN.

Решение: Агент автоматически делает повторные попытки (до 5 раз). Просто подождите.
Ошибка 403 Forbidden:
Google API недоступен в вашем регионе (РФ/РБ).

Решение: Включите VPN перед запуском server.py.
Браузер не открывается:
Убедитесь, что вы установили Playwright (playwright install chromium).

Расширение не открывается:
Проверьте, что сервер запущен (python server.py) и адрес в extension/panel.js указан верно (ws://127.0.0.1:8000/ws).
